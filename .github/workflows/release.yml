name: Routa Release
run-name: >-
  Routa Release ${{
    github.event_name == 'push' && github.ref_name || inputs.version
  }}${{
    github.event_name == 'workflow_dispatch' &&
    (inputs.dry_run && ' [dry-run]' || ' [publish]') ||
    ''
  }}

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Semver version to release, ex: 0.2.4 or v0.2.4"
        required: true
        type: string
      dry_run:
        description: "Build and package only without publishing release assets"
        required: false
        default: false
        type: boolean
      publish_cli:
        description: "Publish npm CLI packages"
        required: false
        default: true
        type: boolean
      publish_harness_monitor:
        description: "Publish Harness Monitor npm packages"
        required: false
        default: true
        type: boolean
      publish_entrix:
        description: "Publish Entrix npm packages"
        required: false
        default: true
        type: boolean
      publish_desktop:
        description: "Publish desktop release assets"
        required: false
        default: true
        type: boolean
      publish_crates:
        description: "Publish Rust crates to crates.io"
        required: false
        default: true
        type: boolean

jobs:
  resolve-version:
    name: Resolve release version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.value }}
    steps:
      - name: Resolve version
        id: version
        env:
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          if [[ -n "${INPUT_VERSION}" ]]; then
            version="${INPUT_VERSION#v}"
          else
            version="${GITHUB_REF_NAME#v}"
          fi

          if [[ -z "${version}" ]]; then
            echo "Unable to resolve release version" >&2
            exit 1
          fi

          echo "value=${version}" >> "$GITHUB_OUTPUT"

  release-desktop:
    name: Release desktop
    needs: resolve-version
    if: |
      github.event_name == 'push' ||
      (
        !inputs.dry_run &&
        inputs.publish_desktop
      )
    permissions:
      contents: write
      actions: read
    uses: ./.github/workflows/tauri-release.yml
    with:
      version: ${{ needs.resolve-version.outputs.version }}
    secrets:
      APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
      APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
      APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
      APPLE_ID: ${{ secrets.APPLE_ID }}
      APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
      APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
      APPLE_API_KEY_CONTENT: ${{ secrets.APPLE_API_KEY_CONTENT }}
      KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
      ROUTA_GITHUB_TOKEN: ${{ secrets.ROUTA_GITHUB_TOKEN }}

  build-desktop:
    name: Build desktop only
    needs: resolve-version
    if: |
      inputs.dry_run
    uses: ./.github/workflows/tauri-build.yml
    with:
      version: ${{ needs.resolve-version.outputs.version }}

  release-cli:
    name: Release CLI
    needs: resolve-version
    if: |
      github.event_name == 'push' ||
      inputs.dry_run ||
      inputs.publish_cli
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/cli-release.yml
    with:
      version: ${{ needs.resolve-version.outputs.version }}
      publish: ${{ github.event_name == 'push' || (!inputs.dry_run && inputs.publish_cli) }}
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      ROUTA_GITHUB_TOKEN: ${{ secrets.ROUTA_GITHUB_TOKEN }}

  release-harness-monitor:
    name: Release Harness Monitor
    needs: resolve-version
    if: |
      github.event_name == 'push' ||
      inputs.dry_run ||
      inputs.publish_harness_monitor
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/harness-monitor-release.yml
    with:
      version: ${{ needs.resolve-version.outputs.version }}
      publish: ${{ github.event_name == 'push' || (!inputs.dry_run && inputs.publish_harness_monitor) }}
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      ROUTA_GITHUB_TOKEN: ${{ secrets.ROUTA_GITHUB_TOKEN }}

  release-entrix:
    name: Release Entrix
    needs: resolve-version
    if: |
      github.event_name == 'push' ||
      inputs.dry_run ||
      inputs.publish_entrix
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/entrix-release.yml
    with:
      version: ${{ needs.resolve-version.outputs.version }}
      publish: ${{ github.event_name == 'push' || (!inputs.dry_run && inputs.publish_entrix) }}
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      ROUTA_GITHUB_TOKEN: ${{ secrets.ROUTA_GITHUB_TOKEN }}

  release-crates:
    name: Publish Rust crates
    needs: resolve-version
    if: |
      github.event_name == 'push' ||
      (
        !inputs.dry_run &&
        inputs.publish_crates
      )
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/cargo-release.yml
    with:
      version: ${{ needs.resolve-version.outputs.version }}
      dry_run: false
    secrets:
      CRATE_TOKEN: ${{ secrets.CRATE_TOKEN }}
